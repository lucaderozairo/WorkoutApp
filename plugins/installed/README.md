# Installed Plugins

Bundled and user-installed plugins. Each plugin is a directory with its own manifest, code, and assets.

Bundled examples:
- `route_planner` — adds route planning to cardio
- `coach_notes` — annotation overlay for video review
- `garmin_sync` — bidirectional Garmin Connect sync
