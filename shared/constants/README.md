# Constants

Application-wide constants with no environmental dependence.

- `units` — supported units and conversion factors
- `sports` — sport identifiers and metadata
- `limits` — max set count, max session duration, etc.
- `defaults` — default UI values
