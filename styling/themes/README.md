# Themes

Concrete value sets for each token, per theme.

- `light`
- `dark`
- `high_contrast`

Themes are switched at runtime via the `profile` feature. The active theme is held in `view_state` and pushed into the rendering context.
