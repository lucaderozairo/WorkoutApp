// Public contract for the messaging feature.
// Messaging is currently view-only — no commands, events, or projections.
// External consumers (message screen, data/mock) need only the UI shapes.

export type { MockUpcomingCall, MockRecentMessage } from './domain/types';
