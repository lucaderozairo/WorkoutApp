# Plugins per Environment

Which plugins are enabled per environment.

```
dev:     [route_planner, coach_notes, garmin_sync, debug_inspector]
staging: [route_planner, coach_notes, garmin_sync]
prod:    [route_planner, garmin_sync]
```
