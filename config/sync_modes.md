# Sync Modes

Per-environment default sync mode.

- `offline_only` — never auto-syncs (manual trigger only)
- `hybrid` — periodic sync, opportunistic (default)
- `live` — continuous via WebSocket

User can override in profile settings.
