# Feature Flags

Runtime toggles. Read at startup; some are hot-reloadable.

## Examples
- `analytics_tab.enabled`
- `social.event_creation.enabled`
- `news_feed.deals.enabled`
- `insights.experimental_models.enabled`
- `dashboard.widget_reorder.enabled`

Flags can be scoped per user, per cohort, or per environment.
