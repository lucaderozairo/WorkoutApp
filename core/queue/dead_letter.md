# Dead Letter

Jobs that exhausted retries land here. Surfaced to the user only if they block functionality. Otherwise visible in `/profile/diagnostics`.

Operators can re-enqueue, edit payload, or delete via the diagnostics screen.
