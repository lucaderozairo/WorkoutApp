# Secure Storage

Abstraction over platform keystore (iOS Keychain, Android Keystore, OS credential vaults on desktop, IndexedDB-with-passphrase on web).

Stores: auth tokens, encryption keys, user secrets. Never raw user data.
