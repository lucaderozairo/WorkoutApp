# Domain Errors

Typed errors raised by feature logic. Each has a stable code so UI can present localized messages.

Examples:
- `INVALID_SET_WEIGHT` — weight outside plausible range
- `SESSION_ALREADY_DELETED`
- `EVENT_CONFLICT` — concurrent edits
- `BLUEPRINT_NOT_FOUND`
